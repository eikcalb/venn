<?php

namespace Venn\handler;

/**
 * Default Class for handling application-wide errors.
 * Custom subclasses can be provided in the <code>app.json</code> file, using the <var>"error_handler"</var> key.
 */
class ErrorHandler {
    public function __construct() {
        ;
    }
}
